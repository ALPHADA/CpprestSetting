cmake_minimum_required(VERSION 3.0.0)
project(basic VERSION 0.1.0)

set (CMAKE_CXX_STANDARD 17)
set (CMAKE_C_STANDARD 17)

include(CTest)
enable_testing()

include_directories(/usr/local/include/)
include_directories(/usr/local/opt/)
include_directories(/usr/local/include/openssl)
include_directories(/usr/local/lib)
include_directories(/usr/local/opt/openssl/include)
set(OPENSSL_ROOT_DIR /usr/local/opt/openssl)

find_package(cpprestsdk REQUIRED)

find_package(Boost COMPONENTS system REQUIRED)
set(CPACK_PROJECT_NAME ${PROJECT_NAME})
set(CPACK_PROJECT_VERSION ${PROJECT_VERSION})
include(CPack)

add_executable(basic main.cpp)
target_link_libraries(basic openssl ${Boost_SYSTEM_LIBRARY} cpprestsdk::cpprest )
# target_link_libraries(basic ${Boost_SYSTEM_LIBRARY} cpprest )
# target_link_libraries(basic PRIVATE cpprestsdk::cpprest)